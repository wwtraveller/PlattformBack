spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:/shop_db
  jpa:
    hibernate:
      ddl-auto: create-drop

s3:
  access-key: test
  secret-key: test
  endpoint: test
  region: test

base:
  url: test